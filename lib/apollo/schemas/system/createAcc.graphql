mutation CREATE_ACC(
    $first_name: String
    $last_name: String
    $email: String!
    $password: String
){
    create_users_item(
        data:{
            first_name: $first_name
            last_name: $last_name
            email: $email
            password: $password
        }
    ){
        id
    }
}
